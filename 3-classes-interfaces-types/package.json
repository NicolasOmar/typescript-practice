{
  "name": "3-classes-interfaces-types",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "tsc && lite-server -c configs/server.json",
    "build:watch": "tsc -w"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
